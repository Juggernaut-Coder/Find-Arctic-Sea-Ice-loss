#ifndef MAIN_H
#define MAIN_H

/* Include required standard library headers. */
#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <ctype.h>
#include <assert.h>
#include <string.h>
#include <search.h>

/* Define system constants. */
#define MAX_BUFFER_SIZE 16
#define FIRST_INPUT_FILE_INDEX 1
#define CORRECT_NUMBER_OF_FILENAME_INPUT 2

/* Include subsystem headers. */
#include "search_arctic_find_match.h"
#include "read.h"

#endif
